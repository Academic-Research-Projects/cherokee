//Client request
//request line
POST / HTTP/1.1
//header
Host: localhost:8080
User-Agent: curl/7.74.0
Accept: */*
Content-Length: 25
Content-Type: application/x-www-form-urlencoded

//end of header
//request body
The text you want to post

// new_file = open(write(request_body + file_extension))
// new_file = write(request_body + file_extension)
// copy file into test_files directory

// ouvrir nouveau fichier
// écrire dans le nouveau fichier
// sauvegarder le fichier dans le repertoire test_files
